# บันทึกการอัปเดต

---

# v0.9.44 - 2026-05-31

## Fixed

- **หน้าเข้าสู่ระบบไม่โชว์ error ดิบของเซิร์ฟเวอร์อีกต่อไป** — ก่อนหน้านี้เมื่อ database เข้าไม่ถึง การเข้าสู่ระบบจะเด้งไปหน้าที่โชว์ error ดิบของ driver (เช่น `pool timeout: failed to retrieve a connection from pool ...`) ทั้งใน URL และในหน้า ตอนนี้ callback ของ NextAuth ดักความผิดพลาดระดับโครงสร้างพื้นฐานแล้วแสดงข้อความที่เป็นมิตรและแปลภาษา ("ขณะนี้ระบบมีปัญหาในการเข้าสู่ระบบ") แทน ส่วน error ดิบจะถูก log ไว้ฝั่งเซิร์ฟเวอร์เท่านั้น โค้ด error ที่ไม่รู้จักจะตกไปข้อความ generic เสมอ รายละเอียดภายในจึงไม่มีทางถึงผู้ใช้

## Changed

- **ทนทานขึ้นเมื่อ database สะดุดชั่วคราว** — ผู้ใช้ที่ล็อกอินอยู่แล้วจะไม่ถูกเตะออกหรือเจอหน้า error หาก database ใช้ไม่ได้ชั่วครู่ การตรวจสอบเซสชันรายคำขอจะ degrade อย่างนุ่มนวลและกลับมาทำงานเองเมื่อ database ฟื้น
- **หน้า error ซ่อนรายละเอียดภายในบน production** — หน้าจอ error ทั้งระดับรวมและของ dashboard แสดงข้อความ generic บน production (ข้อความจริงแสดงเฉพาะตอน development)

---

# v0.9.43 - 2026-05-31

## Fixed

- **บันทึกรายเดือนจำนวนมากไม่พังแล้ว** — ก่อนหน้านี้การบันทึกหลายรายการพร้อมกัน (เช่น 70–100+ รายการตอนสิ้นเดือน) อาจได้ error 500 เพราะ endpoint ดึงบัญชีและหมวดหมู่ของทุกแถวด้วย query แยกกัน *ภายใน* ดาตาเบสทรานแซกชันเดียว ซึ่งเกินลิมิต 5 วินาทีของ interactive transaction เมื่อรายการเยอะ ตอนนี้เปลี่ยนมาเตรียมข้อมูลบัญชี/หมวดหมู่ทั้งหมดล่วงหน้า แล้ว insert แบบ batch ครั้งเดียว (แบ่ง chunk พร้อมตั้ง timeout ชัดเจน) บันทึกหลายร้อยรายการได้เสถียร และฝั่งเซิร์ฟเวอร์ log error จริงแทนการกลืนเป็น 500 เปล่า ๆ

## Added

- **เลือกหลายรายการเพื่อแก้พร้อมกันในหน้าบันทึกรายเดือน** — เลือกหลายแถวที่ยังไม่บันทึก (เช็กบ็อกซ์รายวันเลือกทั้งวัน, "เลือกทั้งหมด" เลือกทุกแถวที่กรอก) แล้วตั้งประเภท หมวดหมู่ หรือบัญชี ทีเดียว หรือลบพร้อมกัน ไม่ต้องแก้ทีละรายการ
- **บันทึกฉบับร่างอัตโนมัติในหน้าบันทึกรายเดือน** — แถวที่ยังไม่บันทึกจะถูกเก็บไว้ในเบราว์เซอร์แยกตามเดือน รีเฟรชหรือออกจากหน้าก็ไม่หาย ระบบกู้คืนให้อัตโนมัติ (พร้อมแจ้งเตือน) และล้างให้เมื่อบันทึกสำเร็จ

## Changed

- **การตรวจสอบหน้าบันทึกรายเดือนชัดเจนขึ้น** — แถวโอนที่ยังไม่เลือกบัญชีปลายทางจะถูกตรวจก่อนบันทึกและไฮไลต์ให้ ถ้าเซิร์ฟเวอร์ปฏิเสธบางแถวจะไฮไลต์และเลื่อนไปให้เห็น และข้อความ error แยกระหว่างปัญหา validation / เซิร์ฟเวอร์ / เครือข่าย

---

# v0.9.42 - 2026-05-16

## Added

- **Tooltip มุมมองเดือน/ปีในปฏิทิน** — `GET /api/transactions/month-summary` และ `year-summary` คืนยอดรวมโดยประมาณเป็น THB ต่อเดือน/ปี (`incomeSumThb`, `expenseSumThb`, `transferSumThb`) ใช้ logic เดียวกับ `calendar-summary` เมื่อ hover ช่องเดือนหรือปีจะแสดงรายละเอียดรายรับ/รายจ่าย/โอน (หรือข้อความไม่มีรายการ)

## Changed

- **ตำแหน่งปุ่มมุมมองปฏิทิน (วัน/สัปดาห์/เดือน/ปี/วันนี้)** — บน **หน้าแรก Dashboard** ปุ่มอยู่ในแถบหัว**ภายในการ์ด** เดียวกับ prev/next และชื่อช่วงเวลา บน **มุมมองปฏิทินในหน้ารายการ** (`/dashboard/transactions?view=calendar`) และหน้า `/dashboard/calendar` (`variant="full"`) ปุ่มอยู่**เหนือการ์ด**แบบเดิม ปรับได้ด้วย prop `viewModeToolbarPlacement` บน `TransactionsCalendar`

---

# v0.9.41 - 2026-05-16

## Changed

- **การ์ดสรุปหน้าแรก** — หน้า Dashboard รวมยอดคงเหลือ รายรับเดือนนี้ และรายจ่ายเดือนนี้เป็นการ์ดเดียว (`DashboardSummaryCard`) พื้นหลังเขียวมะกอกสีเดียว ไม่มีเส้นแบ่งด้านในและไม่มีไอคอนรายรับ/รายจ่าย บนมือถือรายรับกับรายจ่ายเรียงทีละแถว จอ `sm` ขึ้นไปแสดงคู่กัน การซ่อนยอดจากปุ่มใน header ยัง mask ทั้งสามค่าบนการ์ดนี้

---

# v0.9.40 - 2026-05-16

## Added

- **สรุปรายวันปฏิทิน (THB)** — `GET /api/transactions/calendar-summary` คืนจำนวนรายรับ/รายจ่าย/โอน และยอดรวมโดยประมาณเป็น THB ต่อวัน (`incomeSumThb`, `expenseSumThb`, `transferSumThb`) logic ร่วมอยู่ที่ `lib/calendar-summary-thb.ts` ปฏิทินรายการธุรกรรมแสดง tooltip รายละเอียดต่อวัน
- **เลย์เอาต์หน้าสาธารณะ** — `LandingPublicShell` ห่อหน้าแรกและหน้าข้อมูลสาธารณะ (ติดต่อ, นโยบายความเป็นส่วนตัว, ข้อกำหนด, บันทึกเวอร์ชัน) ให้ layout เหมือนกัน `LandingDoodleBackground` เพิ่ม doodle ลอยเบา ๆ และเคารพ `prefers-reduced-motion`

## Changed

- **Timezone ตัวเลือก recurring** — `GET .../link-candidates` รับพารามิเตอร์ `timezone` (ใช้กรอง `onDate` และแสดงวันที่ใน slide picker) ค่าเริ่มต้น `Asia/Bangkok` ถ้าไม่ส่ง

---

# v0.9.39 - 2026-05-07

## Added

- **หน้า Entry แบบแท็บ** — หน้าใหม่ `/dashboard/entry` รวม "บันทึกรายเดือน" และ "บันทึกรายการประจำ" ไว้ในแท็บเดียวกัน สลับระหว่าง 2 flow ได้โดยไม่ต้องออกจากหน้า
- **หน้า Reports แบบแท็บ** — หน้าใหม่ `/dashboard/reports` รวม "สรุป" และ "ประสิทธิภาพการใช้เงิน" ไว้ในแท็บเดียวกัน

## Changed

- **สลับมุมมองหน้ารายการธุรกรรม** — หน้ารายการธุรกรรมสลับระหว่างมุมมองแบบรายการและปฏิทินได้จากหน้าเดียวกัน
- **Sidebar และเมนูล่างมือถือ** — อัปเดตให้มีลิงก์ Entry และ Reports และจัดวางให้เข้าถึงรายการที่ใช้บ่อยได้ง่ายขึ้น

---

# v0.9.38 - 2026-05-06

## Changed

- **ปรับ Hero หน้าแรก** — ปรับ layout และข้อความใหม่บน hero ของหน้าแรก เส้นโค้งพื้นหลังแบบ responsive เสถียรขึ้นทุกขนาดหน้าจอ (ไม่มี layout shift บน viewport แคบ ๆ อีกแล้ว)

---

# v0.9.37 - 2026-05-05

## Added

- **เช็คยอดบัญชี (Balance Reconciliation)** — บันทึกยอดคงเหลือจริงของบัญชี (จากแอปธนาคารหรือ statement) เทียบกับยอดที่แอปคำนวณได้ ระบบเก็บค่าที่ผู้ใช้แจ้ง, ค่าที่แอปคำนวณ, ส่วนต่าง, สกุลเงิน, และหมายเหตุ (ถ้ามี) ทำให้เห็น drift ในระยะยาว
- **ประวัติการเช็คยอด** — หน้ารายละเอียดบัญชีแสดงประวัติการเช็คยอดควบคู่กับรายการธุรกรรม Activity Log บันทึกการเช็คยอดแต่ละครั้ง

## Migration

- เพิ่มตาราง `BalanceReconciliation` ในฐานข้อมูล

---

# v0.9.36 - 2026-05-04

## Added

- **เลือกรายการเดิมแทนสร้างใหม่ (recurring)** — ตอนยืนยัน recurring สำหรับเดือนปัจจุบัน สามารถลิงก์รายการธุรกรรมที่มีอยู่แล้วแทนการสร้างใหม่ได้ Slide picker ใหม่แสดงรายการที่เป็นตัวเลือก (POSTED, ประเภทเดียวกัน, ยังไม่ลิงก์, ไม่อยู่ใน transfer group) พร้อมค้นหาและกรองตามวันที่
- **ยอดคงเหลือหลังแต่ละรายการ** — รายการธุรกรรมเก็บ `accountBalanceAfter` (และ `transferAccountBalanceAfter` สำหรับโอนสกุลเดียวกัน) — ยอดบัญชีทันทีหลังโพสต์รายการนั้น Snapshot จะ rebuild อัตโนมัติหลังแก้ไข ลบ หรือนำเข้า บัตรเดบิตที่ลิงก์บัญชีธนาคารจะได้ snapshot จาก ledger รวมแบบ chronological

## Fixed

- **Cache รายการธุรกรรม** — Endpoint รายการธุรกรรม cache ที่ระดับ module แล้วใช้ payload-version key เมื่อ bump version ระบบจะ invalidate cache เก่าที่ shape JSON ไม่ตรงกับ response ปัจจุบัน ก่อนหน้านี้ cache ค้างอาจทำให้คอลัมน์ "ยอดคงเหลือ" แสดง "—" หลัง deploy
- **PATCH transfer group** — การแก้ไขแถวใน transfer group ที่ส่ง `categoryId: null` (ล้างค่า) ทำงานถูกต้องแล้ว ก่อนหน้านี้ guard อาจสับสนระหว่าง "ล้าง" กับ "ไม่เปลี่ยน" และ reject การแก้ไข
- **Announcement API** — คืน `null` แบบ graceful เมื่อตาราง announcement ยังไม่พร้อมใช้ แทนที่จะ throw

## Migration

- เพิ่มคอลัมน์ `accountBalanceAfter` และ `transferAccountBalanceAfter` ในตาราง `Transaction`

---

# v0.9.35 - 2026-04-26

## Added

- **ค่าเฉลี่ยรายจ่าย** — หน้า Summary และ Spending Efficiency แสดงรายจ่ายเฉลี่ยต่อเดือน / สัปดาห์ / วัน คู่กับยอดรวม ช่วยให้เห็นว่าช่วงเวลาปัจจุบันสูงกว่าหรือต่ำกว่าค่าเฉลี่ยปกติของคุณหรือไม่

---

# v0.9.34 - 2026-04-24

## Added

- **หน้าประสิทธิภาพการใช้เงิน** — หน้าใหม่ที่ `/dashboard/spending-efficiency` เปรียบเทียบรายจ่ายจริงต่อวัน (รวมดอกเบี้ย) กับเป้าหมายต่อวันที่ผู้ใช้ตั้งเอง แสดงส่วนต่างรายวันให้เห็นว่าวันไหนเกินบ้าง
- **หมวดหมู่ที่ยกเว้น** — เลือกหมวดที่ไม่นำมาคำนวณประสิทธิภาพได้ (เช่น ค่าโอน หรือค่าเช่า)
- **API** — `GET /api/spending-efficiency` ใหม่ รับ query `from`, `to`, `timezone`
- **เป้าหมายจดจำได้** — เป้าหมายต่อวันเก็บใน `localStorage` ทำให้คงค่าข้ามการใช้งาน

---

# v0.9.33 - 2026-04-22

## Added

- **ปุ่มซ่อน/แสดงยอดเงิน** — ปุ่มไอคอนตาบน header ของ Dashboard ซ่อนการ์ดยอดคงเหลือ, การ์ดรายรับ/รายจ่าย, และยอดที่ใช้/วงเงินของงบประมาณบนหน้า Dashboard ได้ สถานะจดจำข้ามการรีโหลด (คีย์ `localStorage`: `judtang_balance_visible`, ค่าเริ่มต้นคือแสดง) สะดวกตอนต้องโชว์หน้าจอให้คนอื่นดู

---

# v0.9.32 - 2026-04-22

## Added

- **รายการหลายสกุลเงิน** — บัญชีและรายการธุรกรรมแต่ละรายการมีฟิลด์ `currency` (ISO 4217) แล้ว THB ยังคงเป็นสกุลฐานสำหรับ Dashboard, สรุป, และงบประมาณ ส่วนรายการที่ไม่ใช่ THB จะเก็บจำนวนเงินตามสกุลจริงและแปลงเป็น THB ด้วย `exchangeRate` (= THB ต่อ 1 หน่วยของสกุลในแถวนั้น) และ `baseAmount` ที่บันทึก ณ เวลาโพสต์
- **โอนข้ามสกุลเงิน** — การโอนระหว่างบัญชีที่สกุลเงินต่างกันจะถูกบันทึกเป็นคู่ 2 แถวที่ใช้ `transferGroupId` เดียวกัน (OUT บนต้นทาง, IN บนปลายทาง) แก้ไข/ลบจะมีผลกับทั้งกลุ่ม การโอนสกุลเดียวกันยังคงเป็น 1 แถวเหมือนเดิม
- **API แนะนำอัตราแลกเปลี่ยน** — `GET /api/fx/suggest` ใหม่ คืนค่าอัตรา USD→THB ที่แนะนำ (มี fallback 32 THB/USD เมื่อต้นทางเข้าไม่ถึง)
- **ยอดรวมโดยประมาณบน Dashboard** — Dashboard และ summary คืนค่า THB โดยประมาณสำหรับบัญชีที่ไม่ใช่ THB เพื่อให้ยอดรวมยังมีความหมายเมื่อถือหลายสกุล

## Changed

- **สกุลเงินเปลี่ยนไม่ได้หลังโพสต์** — หลังบัญชีมีรายการ POSTED แรกแล้ว จะเปลี่ยนสกุลเงินของบัญชีนั้นอีกไม่ได้

## Migration

- เพิ่มคอลัมน์ `currency`, `exchangeRate`, `baseAmount`, `transferGroupId`, `transferLeg` ในตาราง `Transaction`; เพิ่ม `currency` ใน `FinancialAccount`

---

# v0.9.31 - 2026-04-05

## Added

- **การ์ดภาพรวมการใช้จ่ายบน Dashboard** — การ์ดใหม่บนหน้า Dashboard แสดงรายจ่ายของวันนี้และรายจ่ายรวมในรอบสัปดาห์ในที่เดียว ข้อมูลรวมอยู่ใน response เริ่มต้นของ Dashboard จึงโหลดมาพร้อมการ์ดสรุปอื่น ๆ

---

# v0.9.30 - 2026-04-04

## Added

- **ตัวเลือกบัญชีค้นหาได้** — `AccountSelectorButton` ใหม่ในฟอร์มธุรกรรม ให้พิมพ์ค้นหาบัญชีตามชื่อได้ และจัดลำดับบัญชีที่ใช้บ่อยขึ้นมาก่อน (MRU เก็บใน `localStorage`)
- **Slide picker เลือกบัญชี** — `AccountSlidePicker` แบบ drill-down ใช้สำหรับเลือกบัญชีในฟอร์มธุรกรรม, dialog ชำระบัตรเครดิต, ฟอร์มบัญชีการเงิน, และ dialog รายการประจำ — ตัวเลือกบัญชีตัวเดียวกันทั่วทั้งแอป

## Changed

- **รีเซ็ตตัวเลือกเมื่อปิด dialog** — เมื่อปิด dialog ที่มีตัวเลือกบัญชี การนำทางและคำค้นจะรีเซ็ต เปิดใหม่จะเริ่มต้นใหม่

---

# v0.9.29 - 2026-04-03

## Changed

- **ปรับปรุงหน้าตั้งงบประมาณ** — หน้า `/dashboard/settings/budget` แยกเป็นคอมโพเนนต์ย่อยที่ชัดเจนขึ้นเพื่อใช้งานง่ายกว่าเดิม:
  - **Toolbar เลือกเดือน/ปี**
  - **การ์ดงบรวม** พร้อมช่องกรอกวงเงินรายเดือน
  - **กริดงบประมาณต่อหมวด** แก้ไขรายหมวดได้ในแถวเดียวกัน
  - **กริด Coverage** แสดงว่าหมวดไหนตั้งวงเงินแล้ว
  - **ส่วนเทมเพลต** สำหรับสร้าง / ใช้ / แก้ไข / ลบเทมเพลต
- **Type ตัวบ่งชี้ความคืบหน้าร่วม** — ใช้ `BudgetProgressIndicator` ร่วมกันทั้งหน้า Dashboard และหน้างบประมาณ ให้เกณฑ์ (normal / warning / critical / full / over) สม่ำเสมอ
- **Loading state ของงบประมาณ** — ปรับตำแหน่ง skeleton และการสื่อสารระหว่างโหลดข้อมูลให้ดีขึ้น

---

# v0.9.28 - 2026-04-03

## Added

- **หมวดหมู่ที่ใช้บ่อย (MRU)** — ตัวเลือกหมวดหมู่จะจดจำและจัดลำดับหมวดที่ใช้บ่อยขึ้นมาก่อน เก็บใน `localStorage` คอมโพเนนต์ใหม่ `CategoryRowSelect` ใช้ในหน้าบันทึกรายเดือนและดร็อปดาวน์หมวดเพื่อความสม่ำเสมอ
- **Redirect การเข้าสู่ระบบที่ปลอดภัยขึ้น** — ผู้ใช้ที่เข้าระบบอยู่แล้วถ้าเข้า `/sign-in`, `/register`, `/restore-account` จะถูกพาไปยังหน้า Dashboard และ redirect หลัง login จะเคารพ `callbackUrl` เฉพาะเมื่อชี้ไปยัง path ที่อยู่ origin เดียวกัน และไม่ใช่หน้า auth อื่น (ป้องกัน open-redirect loop)

## Changed

- **รีเฟรช Dashboard แบบเงียบ** — Quick action, ปฏิทิน, และการเปลี่ยนแปลงรายการธุรกรรม ไม่ทำให้ skeleton หลักกระพริบอีกต่อไป ระบบรีเฟรชข้อมูลในเบื้องหลังโดยที่ข้อมูลเดิมยังแสดงอยู่
- **ตรวจสอบบัตรเดบิต** — การเช็คความสมบูรณ์ของบัตรเครดิตประเภท debit ต้องมี `linkedAccountId` ป้องกันการแจ้งเตือน "บัญชีไม่สมบูรณ์" ผิดพลาด

---

# v0.9.27 - 2026-03-29

## Added

- **ฟอร์มติดต่อสาธารณะ** — หน้าใหม่ที่ `/contact` ให้ใครก็ตาม (ไม่ต้องเข้าระบบ) ส่งข้อความถึงทีมได้ ฟิลด์: หัวข้อ (ทั่วไป / ช่วยเหลือบัญชี / ความคิดเห็นผลิตภัณฑ์ / ความร่วมมือหรือสื่อ / อื่น ๆ), อีเมล, ชื่อ (ไม่บังคับ), เรื่อง, ข้อความ ป้องกันบอทด้วย Cloudflare Turnstile + จำกัดอัตราต่อ IP
- **กล่องข้อความติดต่อ (แอดมิน)** — `/admin/contact-messages` แสดงรายการพร้อมค้นหาและแบ่งหน้า ดูรายละเอียดที่ `/admin/contact-messages/[id]`
- **ส่งอีเมลผ่าน Resend** — เมื่อตั้ง `RESEND_API_KEY` ระบบจะส่งอีเมล transactional (ยืนยันอีเมล, รีเซ็ตรหัสผ่าน, แจ้งเตือนข้อความติดต่อ) ผ่าน Resend ด้วย `EMAIL_FROM` ถ้าไม่ตั้ง จะใช้ Nodemailer ผ่าน SMTP เป็น fallback

## Changed

- **ลืม / รีเซ็ตรหัสผ่าน** — รองรับบัญชี OAuth-only และ email identifier เก่าได้ถูกต้องขึ้น คงพฤติกรรม enumeration-safe เหมือนเดิม
- **URL ฐานในอีเมล** — โหมด development ใช้ `NEXTAUTH_URL` เพื่อให้ลิงก์ verify/reset อยู่บน origin local; production สามารถ override ด้วย `APP_BASE_URL` (ไม่บังคับ)

---

# v0.9.26 - 2026-03-22

## Added

- **อัปโหลดรูปประกาศ** — แอดมินอัปโหลดรูป (JPEG / PNG / WebP) สำหรับประกาศหน้าแรกได้จาก `/admin/settings/announcement` รูปจะแสดงในป๊อปอัปประกาศบนหน้าแรก ใช้ URL แบบ stable ที่ `/storage/announcement/image/<filename>`

---

# v0.9.25 - 2026-03-21

## Changed

- **ป๊อปอัปประกาศหน้าแรก** — ย้ายการตั้งค่าจาก `data/announcement.json` และ `NEXT_PUBLIC_ANNOUNCEMENT_ENABLED` ไปเก็บในตาราง `SiteAnnouncement` แอดมินแก้ไขได้ที่ `/admin/settings/announcement` พฤติกรรม `GET /api/announcement` ด้านผู้ใช้ (ช่วงวันที่, การปิด, คีย์ localStorage) เหมือนเดิม

---

# v0.9.24 - 2026-03-18

## Added

- **รายจ่ายต่อเดือนเฉลี่ย** — แท็บ "ทั้งหมด" ใน `/dashboard/recurring` แสดงสรุปรายจ่ายต่อเดือนเฉลี่ย (รวมรายการ EXPENSE ที่เปิดใช้งาน แปลงตามความถี่เป็นรายเดือน)

## Changed

- **เมนูตัวเลือกต่อรายการ recurring** — ปุ่มบันทึกการจ่ายและแก้ไขรวมอยู่ในเมนู dropdown (⋯) แทนปุ่มแยก กดแล้วเลือก "บันทึกการจ่าย" หรือ "แก้ไข"

---

# v0.9.23 - 2026-03-17

## Added

- **ชื่อหมวดหมู่ภาษาอังกฤษ (nameEn)** — หมวดหมู่รองรับชื่อภาษาอังกฤษแบบ optional เมื่อภาษาของแอปเป็นอังกฤษ ชื่อหมวดหมู่จะแสดงเป็นภาษาอังกฤษเมื่อมี `nameEn`; หมวดหมู่ค่าเริ่มต้นใช้คำแปลในระบบ (เช่น อาหาร → Food) หมวดหมู่ที่สร้างเองสามารถตั้ง `nameEn` ได้เมื่อสร้างหรือแก้ไขใน การตั้งค่า → หมวดหมู่
- **getCategoryDisplayName** — ฟังก์ชันช่วยใน `lib/categories-display.ts` สำหรับแสดงชื่อหมวดหมู่ตาม locale ทั่วทั้งแอป (ฟอร์มรายการ, รายการ, รายการรายเดือน ฯลฯ)

## Changed

- **Category API** — POST และ PATCH `/api/categories` รองรับ `nameEn` (optional) ใน request body
- **ฟอร์มหมวดหมู่** — Dialog เพิ่ม/แก้ไข มีฟิลด์ "ชื่อหมวดหมู่ (ภาษาอังกฤษ)" แบบ optional

---

# v0.9.22 - 2026-03-16

## Added

- **การระงับบัญชี** — ผู้ใช้สามารถระงับบัญชีได้จาก การตั้งค่า → ความเป็นส่วนตัว มีช่วง grace period (ค่าเริ่มต้น 30 วัน ปรับได้ผ่าน `ACCOUNT_GRACE_PERIOD_DAYS`) ก่อนลบถาวร ระหว่าง grace period จะยกเลิกทุกเซสชันและปิดการเข้าสู่ระบบ
- **การกู้คืนบัญชี** — บัญชีที่ถูกระงับสามารถกู้คืนได้ภายใน grace period ผ่าน `/restore-account` (อีเมล + รหัสผ่าน) หลังกู้คืนสำเร็จสามารถเข้าสู่ระบบได้อีกครั้ง
- **นำอีเมลกลับมาใช้ได้หลังลบ** — เมื่อ grace period หมดอายุ อีเมลเดิมสามารถนำไปสมัครใหม่ได้ บัญชีที่ลบจะถูก soft-delete (อีเมลเปลี่ยนเป็น `deleted_<userId>_<email>`) เพื่อเก็บไว้สำหรับ analytics และ audit
- **Activity Log** — เพิ่ม actions: `ACCOUNT_DEACTIVATE`, `ACCOUNT_RESTORE`, `ACCOUNT_DELETED` สำหรับ audit trail
- **หน้าเข้าสู่ระบบ** — ลิงก์ "กู้คืนบัญชีที่ถูกระงับ?" และแถบแจ้งเตือนเมื่อ redirect มาจากการระงับ พร้อมวันที่หมดอายุการกู้คืน

## Changed

- **การเข้าสู่ระบบ** — Credentials และ Google OAuth ตรวจสอบสถานะผู้ใช้แล้ว ผู้ใช้ที่ถูกระงับหรือลบแล้วจะถูกปฏิเสธ JWT refresh ตรวจสอบสถานะและยกเลิกเซสชันที่หมดอายุสำหรับผู้ใช้ที่ถูกระงับ
- **การสมัครสมาชิก** — รองรับผู้ใช้เดิมที่อยู่ใน grace period (409) และเลย grace period (finalize deletion แล้วอนุญาตให้สร้างบัญชีใหม่ด้วยอีเมลเดิมได้)

---

# v0.9.21 - 2026-03-15

## Added

- **ปุ่มกลับขึ้นบนหน้าแรก** — ปุ่มกลมลอยมุมขวาล่างพร้อมไอคอนลูกศรขึ้น แสดงบนหน้า landing สาธารณะ (`/`) เมื่อผู้ใช้เลื่อนลง ปุ่มจะซ่อนเมื่ออยู่ด้านบน; หลังเลื่อนลงเกินระยะหนึ่งจะเลื่อนโผล่จากขวาพร้อม transition ความโปร่งใส กดแล้วเลื่อนกลับขึ้นบนอย่างนุ่มนวล เมื่อกลับถึงบนสุดปุ่มจะเลื่อนกลับและจางออก ใช้ Lucide `ArrowUp` และสไตล์ให้เข้ากับธีมสว่าง/มืดของ landing

---

# v0.9.20 - 2026-03-15

## Added

- **ป๊อปอัปประกาศบนหน้าแรก** — แสดง modal บนหน้าแรกสาธารณะ (`/`) เมื่อตั้ง `NEXT_PUBLIC_ANNOUNCEMENT_ENABLED=true` เนื้อหาอ่านจาก `data/announcement.json` (รูป, หัวข้อ, ข้อความ, ลิงก์ CTA ถ้ามี) ช่วงวันที่ `start_at` / `end_at` กำหนดว่าจะแสดงหรือไม่ ปิดได้ และมีตัวเลือก "ไม่ต้องแสดงอีกในวันนี้" (เก็บใน localStorage ตามวันที่) หรือปิดแบบถาวรเมื่อ `show_once: true`
- **เนื้อหาประกาศหลายภาษา** — ใน config ฟิลด์ `title`, `content`, `image_alt`, `action_label` รองรับไทยและอังกฤษแบบ `{ th, en }` ระบบจะเลือกข้อความตามภาษาของแอป

## Changed

- **ตำแหน่งแสดงประกาศ** — แสดงเฉพาะหน้าแรก (`/`) ไม่แสดงใน dashboard โดย component ถูก mount ใน root layout

---

# v0.9.19 - 2026-03-15

## Added

- **การยินยอมคุกกี้ (Cookie consent)** — แถบแจ้งเตือนคุกกี้แสดงเมื่อเข้าใช้งานครั้งแรก ผู้ใช้สามารถกด "ยอมรับทั้งหมด" (เปิด analytics) หรือ "จัดการการตั้งค่า" เพื่อเลือก: คุกกี้จำเป็น (เปิดตลอด) และ คุกกี้วิเคราะห์ (เลือกได้) การตั้งค่าถูกเก็บใน localStorage
- **Analytics แบบมีเงื่อนไข** — Vercel Analytics และ Speed Insights โหลดเฉพาะเมื่อผู้ใช้ยินยอมให้ใช้ analytics ไม่มีการติดตามก่อนได้รับความยินยอม

## Changed

- **การโหลด Analytics** — เปลี่ยนจากการใส่ Analytics/SpeedInsights ใน layout โดยตรง เป็น component `ConditionalAnalytics` ที่ตรวจสอบ consent ก่อนแสดงผล

---

# v0.9.18 - 2026-03-14

## Added

- **เมนูตัวเลือกต่อรายการแจ้งเตือน** — แต่ละแถวการแจ้งเตือนมีปุ่ม "⋯" (ตัวเลือกเพิ่มเติม) มุมขวาบน แตะแล้วมีเมนู "ทำเครื่องหมายว่าอ่านแล้ว" หรือ "ทำเครื่องหมายว่ายังไม่อ่าน" ตามสถานะปัจจุบัน
- **ทำเครื่องหมายว่ายังไม่อ่าน** — การแจ้งเตือนแบบ persisted สามารถเปลี่ยนกลับเป็นยังไม่อ่านได้ผ่านเมนูต่อรายการ การแจ้งเตือนแบบ virtual ก็สามารถ "ยกเลิกการปิด" ได้เช่นกัน (ลบออกจาก localStorage)
- **PATCH /api/notifications/read** — รองรับ `{ ids: string[], unread: true }` สำหรับทำเครื่องหมายว่ายังไม่อ่าน

## Changed

- **Badge แจ้งเตือนเมื่อโหลดหน้า** — ตัวเลขยังไม่อ่านโหลดเมื่อเปิดหน้าแล้ว แสดงได้โดยไม่ต้องเปิด panel ก่อน
- **สถานะอ่านของ virtual alert** — การแจ้งเตือนแบบ virtual (รายการประจำ งบประมาณ ฯลฯ) เก็บสถานะ "อ่านแล้ว" ใน `localStorage` เพื่อให้ยังแสดงว่าอ่านแล้วหลัง refresh
- **การแจ้งเตือนบนมือถือ** — ใช้ Sheet (drawer เลื่อนจากล่าง) แบบเต็มจอแทน Popover เพื่อ UX ดีขึ้นบนหน้าจอเล็ก
- **Accessibility ของ Sheet** — เพิ่ม SheetHeader พร้อม SheetTitle และ SheetDescription (sr-only) สำหรับ screen reader

---

# v0.9.17 - 2026-03-11

## Added

- **Vercel Speed Insights** — เพิ่ม `@vercel/speed-insights/next` ใน root layout เพื่อเก็บข้อมูลประสิทธิภาพจากผู้ใช้จริง (Core Web Vitals) สำหรับการติดตามและปรับปรุง

---

# v0.9.16 - 2026-03-09

## Changed

- **คง draft ของ slip upload ไว้ได้** — draft และผล OCR ของ slip upload ถูกเก็บใน `localStorage` แล้ว จึงปิด dialog ไปทำอย่างอื่นก่อน แล้วกลับมาเปิดต่อภายหลังได้โดยไม่เสีย batch เดิม
- **ปิด dialog แล้วงานยังทำต่อได้** — การปิด dialog จะไม่ abort งาน OCR/upload ที่กำลังทำอยู่ หากยังอยู่หน้าเดิม และเมื่อเปิดกลับมาจะเห็นสถานะล่าสุดของแต่ละ slip
- **กู้คืนงานหลัง refresh หน้า** — draft ที่เสร็จแล้วหรือ error จะถูก restore หลัง refresh หน้า ส่วน draft ที่ยังประมวลผลไม่เสร็จจะถูก restore เป็นสถานะงานถูกขัดจังหวะเพื่อให้ตรวจสอบหรืออัปโหลดใหม่อย่างปลอดภัย
- **ปรับ UI ตรวจสอบ slip** — ระหว่างกำลังโหลด การ์ด slip จะยังไม่แสดง summary/edit controls; หลัง OCR เสร็จจึงค่อยแสดงสรุป `จำนวนเงิน`, `ประเภท`, `วันที่` และซ่อน field ขั้นสูงไว้หลังส่วนแก้ไขแบบขยายได้
- **เพิ่ม telemetry ระหว่างอัปโหลด** — dialog แสดงเปอร์เซ็นต์อัปโหลด, ขนาดที่อัปโหลด/ทั้งหมด, ความเร็วอัปโหลดโดยประมาณ, ขนาดไฟล์ก่อน/หลัง compress, เวลาแต่ละ slip และเวลารวมทั้ง batch
- **ปรับ action ของ slip upload** — ในโหมด preview สามารถเพิ่ม slip เข้า batch เดิมได้ตามสถานะที่เหมาะสม และเมื่อเคลียร์ draft หมดก็ยังมี empty state ที่ใช้งานต่อได้ทันที

---

# v0.9.15 - 2026-03-09

## Changed

- **หน้า Recurring บนมือถือ** — การ์ดรายการใน `/dashboard/recurring` ปรับเป็น 2 แถวบนหน้าจอเล็ก เพื่อให้อ่านรายละเอียดได้ง่ายขึ้น และไม่ให้จำนวนเงินกับปุ่ม action เบียดกัน
- **ช่องวันที่ใน dialog บันทึกการจ่าย** — วันที่จ่ายใน recurring confirm dialog เปลี่ยนมาใช้ปุ่มเปิดปฏิทินแบบ inline เหมือน transaction dialog แทน input วันที่แบบ native
- **สวิตช์สถานะใน recurring settings** — ใน dialog สร้าง/แก้ไข recurring ฟิลด์สถานะเปิดใช้งานในโหมดแก้ไขเปลี่ยนจาก checkbox เป็น toggle switch
- **สไตล์ปุ่มบันทึกการจ่าย** — ปุ่ม `บันทึกการจ่าย` บนการ์ด recurring เปลี่ยนเป็นปุ่ม solid สีเขียวแทน outline เพื่อเน้น action หลัก

---

# v0.9.14 - 2026-03-05

## Added

- **งบประมาณ: สร้าง/แก้ไข/ลบเทมเพลตในตั้งค่า** — ในหน้างบประมาณ (`/dashboard/settings/budget`) มีส่วน Templates ให้สร้าง รายการ แก้ไข และลบเทมเพลตงบได้ แต่ละเทมเพลตมีชื่อ งบรวม (ถ้าต้องการ) และรายการงบต่อหมวด (หมวด + จำนวน) แบบเพิ่มแถวได้ Dialog สร้าง/แก้ไขรองรับหลายแถวหมวดพร้อมปุ่มเพิ่ม/ลบ; ลบเทมเพลตมี dialog ยืนยัน การ์ดใช้เทมเพลตยังแสดงเมื่อมีเทมเพลต
- **งบประมาณ: แก้ไขงบต่อหมวด** — แต่ละแถวงบหมวดของเดือนที่เลือกมีปุ่มแก้ไข (ไอคอนดินสอ) แตะแล้วเปิด dialog แก้จำนวนงบ (฿) ของหมวดนั้น บันทึกเรียก `PATCH /api/budgets/categories/[id]` และโหลดความคืบหน้าใหม่ ไม่ต้องลบแล้วเพิ่มใหม่เพื่อเปลี่ยนวงเงิน

## Changed

- **i18n ตั้งงบประมาณ** — คีย์ใหม่ภายใต้ `settings.budget`: `createTemplate`, `editTemplate`, `deleteTemplate`, `templateDeleteConfirm`, `editCategoryBudget`, `addLimitRow` (EN/TH)

---

# v0.9.13 - 2026-03-05

## Changed

- **โฟกัส input ใน dialog เต็มจอบนมือถือ** — เมื่อ dialog เป็น fullscreen บนมือถือ การโฟกัสที่ input หรือ textarea ใดๆ (เช่น วันที่ บัญชี จำนวนเงิน หมายเหตุ) จะเลื่อนพื้นที่ scroll ของ dialog ให้ช่องที่โฟกัสอยู่ใน view อัตโนมัติ ทำใน `DialogBody` (`components/ui/dialog.tsx`) เพื่อให้ทุก dialog ที่ใช้ (ฟอร์มธุรกรรม ฟอร์มบัญชี หมวดหมู่ ชำระบัตร เครดิต โปรไฟล์ ฯลฯ) ได้พฤติกรรมนี้โดยไม่ต้องแก้ทีละ dialog

---

# v0.9.12 - 2026-03-05

## Changed

- **รองรับ Dark theme ครบถ้วน** — คอมโพเนนต์ องค์ประกอบ และหน้าต่างๆ รองรับโหมดมืดครบ:
  - **เมนูล่างมือถือ** — สีปรับตาม theme (พื้นหลัง ขอบ สถานะเลือก/ไม่เลือก) ผ่าน `useTheme()`
  - **หน้า Landing** — เพิ่ม CSS variables `.dark .landing-page` สำหรับโหมดมืด
  - **ปุ่ม CTA หน้า Landing** — Dark variants สำหรับปุ่มเริ่มใช้งานและปุ่มหลัก
  - **หน้ารายการบัญชี** — ปุ่มเพิ่มบัญชีและไอคอนมี dark variants
  - **Card type select** — ไอคอนใช้ `dark:text-stone-400`
  - **Dialog เมนู sidebar บนมือถือ** — รายการที่เลือกใช้ `dark:bg-amber-900/50 dark:text-amber-100`
  - **ปฏิทินรายการ** — ข้อความจางใช้ `dark:text-stone-500` เพื่อ contrast ดีขึ้น
  - **Bank combobox** — ข้อความไม่พบผลลัพธ์มี dark variant

---

# v0.9.11 - 2026-03-04

## Added

- **เมนูล่างมือถือออกแบบใหม่** — พื้นหลังครีม ไอคอน + ข้อความต่อรายการ แถบแสดงสถานะเมื่อเลือก; 5 รายการ: หน้าหลัก บัญชี รายการ สรุป ตั้งค่า
- **Hook useIsDesktopOrLarger** — ตรวจจับ viewport ≥ 1024px (Tailwind `lg`) สำหรับ layout เดสก์ท็อป
- **ตารางรายการแบบกระชับ (tablet)** — บนจอ < 1024px: วันที่ + บัญชี + หมวดหมู่รวมในคอลัมน์เดียว; ซ่อนคอลัมน์ประเภท; จำนวนเงินแสดงด้วยสี (เขียว/แดง/น้ำเงิน); แตะแถวเพื่อเปิดเมนูแก้ไข/ลบ
- **เมนูดำเนินการพร้อมรายละเอียด** — เมื่อแตะรายการบน tablet dialog จะแสดงวันที่ บัญชี และจำนวนเงินก่อนปุ่มแก้ไข/ลบ

## Changed

- **รายการล่าสุดบน Dashboard (มือถือ)** — แสดงเฉพาะไอคอน (ไม่มี label รายรับ/รายจ่าย); วันที่แยกอีกบรรทัดด้วย `text-[10px]`
- **ตารางรายการ (desktop)** — ปุ่มแก้ไข/ลบยังอยู่ในตาราง; layout กระชับและเมนูแบบแตะใช้เฉพาะ tablet/mobile
- **i18n รายการธุรกรรม** — เพิ่มคีย์ `dateAndAccount`, `tapToEditOrDelete`, `selectAction`

---

# v0.9.10 - 2026-03-04

## Added

- **Sidebar แบบ responsive** — บนหน้าจอเล็ก (< 640px) sidebar เปิดเป็น dialog พร้อมเมนูนำทางแบบตาราง 2×2 เพื่อให้แตะใช้งานง่ายขึ้น
- **เมนูล่างบนมือถือ** — แถบนำทางคงที่ด้านล่างบนมือถือ (< 768px) พร้อมลิงก์ด่วนไป Dashboard บัญชี และปฏิทิน
- **Hook useIsSmallScreen** — ตรวจจับ viewport ต่ำกว่า Tailwind sm (640px) สำหรับปรับ UI แบบ mobile-first
- **Dashboard layout แบบ responsive** — Layout แบบคอลัมน์เดียวบนหน้าจอเล็ก; ข้อความบนปุ่มขึ้นบรรทัดใหม่เพื่อไม่ให้ล้น

## Changed

- **สไตล์ปุ่มบน Dashboard** — ปรับปรุงการตอบสนองทางสายตาและ accessibility ด้วยขอบและเงาที่ดีขึ้น
- **คอมโพเนนต์ Dialog** — ปรับสไตล์เนื้อหาให้เหมาะกับ overlay เมนู sidebar บนหน้าจอเล็ก
- **การจัดการบัญชี** — ปรับปรุงดีไซน์ปุ่มเพิ่มบัญชี; รองรับประเภทบัญชีเริ่มต้นในโมดัลสร้างบัญชี

---

# v0.9.9 - 2026-03-03

## Added

- **ปุ่มเพิ่มรายการด่วนบน Dashboard** — การ์ดยอดคงเหลือและการ์ดเพิ่มรายการด่วนย้ายไปอยู่เหนือปฏิทินในคอลัมน์ซ้าย ปุ่มเพิ่มรายการด่วนมี บันทึกรายรับ บันทึกรายจ่าย และโอนระหว่างบัญชี — แต่ละปุ่มเปิดฟอร์มธุรกรรมพร้อมประเภทที่เลือกไว้แล้ว
- **ลิงก์จัดการบัญชี** — การ์ดเพิ่มรายการด่วนมีลิงก์ไปหน้ารายการบัญชีเพื่อเข้าถึงได้เร็ว

## Changed

- **ปรับ layout หน้า Dashboard** — การ์ดสรุปออกแบบใหม่: การ์ดยอดคงเหลือใช้สไตล์เขียวเข้มเด่นชัด; การ์ดรายรับและรายจ่ายใช้พื้นหลังเขียวอ่อนและแอมเบอร์พร้อมวงกลมตกแต่ง จัด layout เป็นสองคอลัมน์: ซ้าย (ยอดคงเหลือ + ปุ่มเพิ่มรายการด่วน + ปฏิทิน), ขวา (รายรับ + รายจ่าย + รายการล่าสุด)

---

# v0.9.8 - 2026-03-03

## Added

- **จดจำฉัน** — เข้าสู่ระบบด้วยตัวเลือก "จดจำฉัน" เพื่อขยายระยะเวลาเซสชัน (ค่าเริ่มต้น 30 วันเมื่อติ๊ก; 24 ชั่วโมงเมื่อไม่ติ๊ก)
- **ข้อกำหนดและเงื่อนไข** — หน้าเว็บสาธารณะที่ `/terms` ครอบคลุมข้อกำหนดการให้บริการ การใช้งานที่ยอมรับได้ และภาระผูกพันของผู้ใช้ มีลิงก์ในส่วน Footer
- **ช่วยเหลือและความคิดเห็น** — ส่งรายงานบั๊กและคำขอฟีเจอร์ได้ที่ Settings → Help & Feedback; แอดมินจัดการที่ `/admin/reports`
- **ปรับโครงสร้างประเภทบัตร** — เปลี่ยนจาก `cardType` เป็น `cardAccountType` (credit/debit/prepaid/other) และ `cardNetwork` (visa/master/jcb/amex/unionpay/truemoney/other) เพื่อจัดหมวดหมู่ชัดเจนขึ้น
- **โลโก้ธนาคารและไอคอนเครือข่ายบัตร** — หน้ารายการบัญชีและฟอร์มแสดงโลโก้ธนาคารและไอคอนเครือข่ายบัตรเพื่อระบุตัวตนได้ง่ายขึ้น
- **AccountCombobox และ CardTypeSelect** — คอมโพเนนต์ใหม่สำหรับการเลือกบัญชีและการจัดการประเภทบัตรในฟอร์ม

## Changed

- **ดีไซน์ Earth tone** — หน้าแรก หน้า auth และ Dashboard อัปเดตด้วยโทนสี stone/amber/emerald ให้สอดคล้องกัน
- ดร็อปดาวน์และฟอร์มบัญชีการเงินปรับให้เข้ากับระบบดีไซน์ใหม่

## Migration

- เพิ่มฟิลด์ `cardAccountType` และ `cardNetwork` ใน FinancialAccount; `cardType` ถูกเลิกใช้

---

# v0.9.7 - 2026-03-02

## Added

- **เข้ารหัสเลขบัญชี/บัตร** — เลขบัญชีเต็ม (โหมด FULL) ถูกเข้ารหัสด้วย AES-256-GCM ก่อนเก็บในฐานข้อมูล
- **โหมดเก็บเลขบัญชี** — บัญชีธนาคารและกระเป๋าเงินเลือกได้: เก็บเลขเต็ม (เข้ารหัส) หรือเก็บแค่ 4 ตัวท้าย
- **บัตรเครดิตกรอก 4 ตัวท้าย** — กรอกเฉพาะเลข 4 ตัวท้ายของบัตร ไม่เก็บเลขเต็ม

## Changed

- **การลบบัญชี/บัตร** — ใช้รหัสสุ่ม 6 ตัวยืนยันการลบทุกกรณี (แทนการกรอกเลขบัญชี)
- ปุ่มแสดงเลขเต็ม (eye) แสดงเฉพาะบัญชีที่เก็บเลขเต็ม (โหมด FULL) เท่านั้น

## Migration

- เพิ่มฟิลด์ `accountNumberMode` และเปลี่ยน `accountNumber` เป็น TEXT สำหรับเก็บข้อมูลเข้ารหัส
- ต้องตั้ง `ENCRYPTION_KEY` ใน `.env` เพื่อใช้โหมดเก็บเลขเต็ม (สร้าง key: `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`)

---

# v0.9.6 - 2026-03-02

## Fixed

- การ์ดสรุป **"เดือนนี้"** (รายรับ / รายจ่าย / ยอดคงเหลือ) และ **รายการธุรกรรมล่าสุด** บนหน้า Dashboard อัปเดตทันทีหลังเพิ่ม แก้ไข หรือลบธุรกรรมจากปฏิทิน — ก่อนหน้านี้ข้อมูลยังคงเป็นค่าเก่าแม้จะ refresh แล้ว เนื่องจาก API ใช้ stale-while-revalidate cache

---

# v0.9.5 - 2026-03-02

## Fixed

- การ์ดสรุป **"เดือนนี้"** บนหน้า Dashboard แสดงรายรับ รายจ่าย และยอดคงเหลือ**เฉพาะเดือนปัจจุบัน**อย่างถูกต้องแล้ว — ก่อนหน้านี้จะรวมยอดจากทุกธุรกรรมตลอดเวลา
- ยอดเงินและจำนวนธุรกรรมในหน้า **บัญชี** อัปเดตทันทีหลังสร้าง แก้ไข หรือลบธุรกรรม — ไม่ต้อง refresh หน้าอีกต่อไป
- **มุมมองปฏิทิน** (จุดบนปฏิทินและรายการธุรกรรมในหน้าต่างวัน) รีเฟรชทันทีหลังเพิ่ม แก้ไข หรือลบธุรกรรม
- **รายการธุรกรรม** ในหน้า Transactions ก็แสดงการเปลี่ยนแปลงทันทีเช่นกัน
- การปิดรอบบัญชีบัตรเครดิตอัปเดตข้อมูล Statement ของบัญชีได้ถูกต้องโดยไม่ต้อง refresh หน้า

---

# v0.9.4 - 2026-03-02

## Added

- เพิ่มหน้า **นโยบายความเป็นส่วนตัว** สาธารณะที่ `/privacy` — เข้าถึงได้โดยไม่ต้องเข้าสู่ระบบ ครอบคลุมข้อมูลที่เก็บรวบรวม วิธีการใช้ข้อมูล บริการของบุคคลที่สาม (รวมถึง Cloudflare Turnstile) นโยบายการเก็บรักษาข้อมูล สิทธิ์ของผู้ใช้ และการลบบัญชี
- เพิ่มลิงก์ไปยังหน้านโยบายความเป็นส่วนตัวในส่วน Footer ของหน้าแรก

---

# v0.9.3 - 2026-03-02

## Added

- เพิ่มหน้า **Releases** สาธารณะ — ทุกคนสามารถดูสิ่งที่อัปเดตได้โดยไม่ต้องเข้าสู่ระบบ
- เพิ่มหน้าแนะนำแอปสำหรับผู้เยี่ยมชม พร้อมตัวเลือกธีม (สว่าง / มืด / ตามระบบ) และตัวเลือกภาษา (ไทย / อังกฤษ)

---

# v0.9.2 - 2026-03-02

## Improved

- หน้า Dashboard โหลดเร็วขึ้นอย่างเห็นได้ชัด รอน้อยลง ใช้งานได้เลย

---

# v0.9.1 - 2026-03-02

## Added

- ดู **บันทึกการอัปเดต** ได้โดยตรงในแอป ที่ Settings → Patch Notes เพื่อติดตามสิ่งที่เปลี่ยนแปลงเสมอ

---

# v0.9.0 - 2026-03-01

## Added

- **โอนเงินระหว่างบัญชี** — บันทึกการโอนเงินจากบัญชีหนึ่งไปยังอีกบัญชีได้แล้วในประเภท "โอน"
- ยอดคงเหลือในบัญชีคำนวณรวมรายการโอนด้วย และสามารถกรองในรายการธุรกรรมได้
- การนำเข้าไฟล์ CSV รองรับรายการโอนเงินแล้ว

## Migration

- ฟีเจอร์โอนเงินต้องการเพิ่มข้อมูลในฐานข้อมูลเพื่อเชื่อมโยงบัญชีต้นทางและบัญชีปลายทางของแต่ละรายการโอนเข้าหากัน

---

# v0.8.0 - 2026-03-01

## Added

- **หมวดหมู่ธุรกรรม** — จัดระเบียบรายรับและรายจ่ายด้วยหมวดหมู่เริ่มต้น หรือสร้างหมวดหมู่เองได้ใน Settings
- **ซ่อนบัญชี** — ซ่อนบัญชีที่ไม่ต้องการใช้จากฟอร์มธุรกรรมได้โดยไม่ต้องลบทิ้ง
- บันทึกกิจกรรมตอนนี้ติดตามการสร้าง แก้ไข หรือลบบัญชีด้วย

## Migration

- ฟีเจอร์ซ่อนบัญชีต้องการเพิ่มข้อมูลในฐานข้อมูลเพื่อจดจำว่าบัญชีไหนที่คุณเลือกซ่อนไว้

---

# v0.7.0 - 2026-03-01

## Added

- **รองรับบัตรเครดิต** — ติดตามค่าใช้จ่าย การชำระเงิน และรอบบัญชีของบัตรเครดิต
- ตั้งค่าวงเงิน วันปิดรอบบัญชี วันครบกำหนดชำระ และอัตราดอกเบี้ยได้ต่อบัตร
- รองรับบัตรหลายประเภท: Visa, Mastercard, JCB, Amex, UnionPay, TrueMoney
- เลือกธนาคารไทยจากรายการดร็อปดาวน์เมื่อเพิ่มบัตร
- หมายเลขบัตรและเลขบัญชีถูกปิดบังเพื่อความปลอดภัย

## Migration

- ฟีเจอร์บัตรเครดิตต้องการโครงสร้างฐานข้อมูลใหม่เพื่อเก็บข้อมูลบัตร (วงเงิน, อัตราดอกเบี้ย, ประเภทบัตร, ธนาคาร, เลขบัญชี) และเพื่อติดตามรอบบัญชีรายเดือนและสถานะของธุรกรรม

---

# v0.6.0 - 2026-02-26

## Added

- **ยืนยันอีเมล** — หลังสมัครสมาชิก ระบบจะส่งอีเมลยืนยันให้ หน้าโปรไฟล์จะแสดงสถานะการยืนยัน และสามารถขอส่งอีเมลใหม่ได้หากจำเป็น

---

# v0.5.0 - 2026-02-26

## Added

- **ลืมรหัสผ่าน?** — รีเซ็ตรหัสผ่านผ่านอีเมลได้แล้วจากหน้าเข้าสู่ระบบ

---

# v0.4.0 - 2026-02-26

## Added

- **บันทึกรายรับและรายจ่าย** — สร้าง แก้ไข และลบธุรกรรมผ่านหน้าต่างที่ใช้งานง่าย กรองตามช่วงวันที่และประเภทได้
- **มุมมองปฏิทิน** — ดูรายรับรายจ่ายแบบรายวัน และเพิ่มหรือแก้ไขธุรกรรมได้โดยตรงจากปฏิทิน
- **การ์ดสรุปข้อมูล** บนหน้า Dashboard แสดงภาพรวมการเงินของคุณในทันที
- **ส่งออก** ธุรกรรมเป็นไฟล์ CSV พร้อมตัวกรองตามวันที่และประเภท

---

# v0.3.0 - 2026-02-21

## Added

- **บันทึกกิจกรรม** — ดูประวัติการดำเนินการต่าง ๆ ในบัญชีของคุณ เช่น การเข้าสู่ระบบ การเปลี่ยนรหัสผ่าน การแก้ไขธุรกรรม ได้ที่ Settings → Activity Log
- มีตัวกรองเพื่อค้นหากิจกรรมตามประเภทหรือวันที่

---

# v0.2.0 - 2026-02-14

## Added

- **จัดการเซสชัน** — ดูและยกเลิกเซสชันที่ใช้งานอยู่ได้จากการตั้งค่าบัญชี
- **หน้าโปรไฟล์** — แก้ไขชื่อที่แสดง หรือเปลี่ยนรหัสผ่านได้ที่ Dashboard → Profile
- ระบบบันทึกเซสชันแยกตามอุปกรณ์ ทำให้เห็นว่าคุณเข้าสู่ระบบจากที่ไหนบ้าง และสามารถออกจากระบบจากระยะไกลได้

---